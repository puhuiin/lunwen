# 接力提示词（新窗口直接粘贴本文件全文即可续作）

你正在接续一个已进行多轮的**中国基金经理行为分析学术论文**项目。请先完整阅读 `D:\Desktop\基金经理行为分析研究\handoff.md`（重点：一、二、三、四章状态、Phase 2.12–2.17、五文件清单），以及 `独立学术审查报告.md`、`学术审查与修订方案.html`。读完后按本提示词继续。

## 项目定位（不可回退的共识）
- 论文已重构为**"预测框架"**：因变量 `ff5_adj_return`（FF5 alpha）对每只基金是**常数**（time_invariant_share=1.0，HARD判定 PANEL_IS_CROSS_SECTION），核心 M0-M4 本质是**截面回归**，有效N=297。
- 采用**三重识别策略**：截面基准(N=297) / 前向预测(未来1&4季度) / 组内双向FE(excess_return)。
- **证据分级**：RiskAsym、LSV = B预测级（截面+前向稳健）；DE = B+（唯一组内效应，t=-3.64，反向处置效应PGR0.891<PLR0.919）；描述性发现=C。
- **严禁因果越界**：Hansen J 已拒绝IV外生性，不得写"X导致Y/因果效应"；只用"相关/可预测/最接近因果的组内证据"等措辞。任何B/C级结论出现因果动词即视为错误。

## 核心数字（已独立复算，勿改）
L5增量R²=0.073(34.9%)；截面N=297: RA t=5.63 / LSV t=4.81 / DE t=-1.76；前向4季度 RA t=4.95；择时控制后 RA t=5.77；组内 DE t=-3.64；基金bootstrap(2000次) RA 3.95 / LSV 4.17；IPW后 LSV 5.04 / RA 2.99。

## 已完成（勿重复）
独立审查+P0重构(Ch4/5/6)、Ch1/3对齐、摘要(中英文)、Ch2织入新文献、230篇补充文献核验归档(13篇英文核心已验证,manifest.csv)、全文因果越界扫描修复、P1-2两阶段bootstrap(§6.4.7)、P1-3 IPW(§4.3)。

## 剩余任务（按优先级，从最高开始做）
1. **P1 扩展规范曲线+预注册**：回应v13→v23 specification漂移。把规范曲线从7设定扩展到全部合理设定（缩尾/聚类/因子模型/控制集组合），报告RA/LSV/DE显著占比与方向一致率，写入§4.9。
2. **P2 §4.2.3补RiskAsym期限异质**：补一句解释"4季度前向为正、1季度为负/不显著"的期限结构（与Gutierrez-Kelley 2009长期反转呼应）。
3. **P2 组内lsv设定敏感注明**：§4.2.4注明lsv组内显著性对控制集敏感（无控制p=0.04 vs 含控制ns）。
4. **P2 用206篇中文文献充实§2.4**：从`参考文献\补充文献`（_分类清单.csv）选羊群/锦标赛/处置效应中文文献补入中国情境。
5. **可选 全文整合投稿稿**：Ch1-6+摘要合并为单一docx（用docx技能）。
6. **P1 幸存者偏差（数据受阻）**：需高权限tushare或Wind清盘基金净值；当前token无fund_nav/fund_basic权限。若用户提供了新数据源再做；否则维持§6.4.1披露。

## tushare
token=4eef1912524641bb38e51c92562652a5fe1ed6d5b940da586eead0a2 （**敏感，勿提交git、勿外传**）。已验证：股票`daily`可用；**`fund_basic`/`fund_nav`无权限**（需更高积分）。`ts.set_token(token); pro=ts.pro_api()`。

## 数据/代码位置
- 主面板 `数据\mvp_panel_v20.csv`(9581×154)；清盘候选 `数据\清盘候选_偏股基金列表.csv`。
- 审查脚本在技能目录 `empirical-paper-independent-audit\scripts\`（diagnose_structure/recompute/corrective_regressions）。
- 工作脚本在 `C:\Users\26955\.qwenworkcn\workspace\msjv4lh0fw43xm7g\`（p1_fixes.py/bootstrap_xsec.py/prep_panel.py等）。

## 环境坑（必读）
- 数据读取/回归**一律写.py脚本执行**，禁止PowerShell内联解析（$变量会被cmd剥离）。
- `corrective_regressions.py`需数值时间索引：date为字符串时linearmodels报错，先加 `t=year*12+month` 列（见prep_panel.py）。
- 两阶段alpha bootstrap取截距SE用 `bse[0]`（非bse[1]）；截面回归须 `dropna(subset=含alpha)` 否则系数NaN。
- 扫描版PDF无文字层，verify_pdf.py会判FAIL；用 `pip install pymupdf` 后渲染首页PNG再目视核验。
- 用户下载的PDF文件名常与内容不符，归档/引用前必须逐篇内容验证。

## 工作方式
- 每完成一个任务，更新 handoff.md（新增Phase）并在正文落点。
- 保持"预测框架"叙事与证据分级；新增任何结论先自问证据等级，越界即改。
- 需要核验论文PDF用 academic-paper-download-verify 技能；需要复算/挑刺用 empirical-paper-independent-audit 技能。

**现在从剩余任务#1（扩展规范曲线+预注册）开始。**
