# 数据扩展与下载执行报告（2026-08-14）

> 任务：按 `数据下载提示词_2026-08-13.md` 补齐外部数据缺口，并把 TO、de 等字段扩到全样本（400 只 / 2006Q2–2026Q2，9974 观测）。
> 环境：本沙箱**无 CSMAR/Wind 终端、无 iFinD 授权**；AkShare 东财接口经核验不可靠（提示词已禁止进主回归）。
> 结论先行：**本地可最大化部分已全部完成并落盘；需 CSMAR/Wind/iFinD 授权的下载项已留好落盘位与精确提示词，待你在授权终端执行。**

---

## 一、本次已完成（本地最大化，全部落盘）

| 字段 | 处理动作 | 覆盖（重跑后，权威见主面板 `主分析面板_重建.csv`） | 说明 |
| --- | --- | --- | --- |
| **TO_two_sided / OCI** | **恢复被损坏的真实基座** | 18.0% 观测 / **50% 基金**（200/400） | 先前一次重生成把 `total_buy/total_sell` 全置 NaN、TO 改填东财不可靠序列；已从备份恢复真实买卖金额（校验 `TO=(buy+sell)/(2·aum)` 中位比=1.0）。**无法从免费源扩到 400 只**——见第三节 P1。 |
| **ICI / industry_hhi** | **从全持仓重算（全样本）** | **94.3% 观测 / 100% 基金**（400/400） | 旧 HHI 成品仅 357 只 / 2018–2025；改用 `基金持仓明细_全量修正版_v2.csv`（446 只 / 全季度）按原口径（申万31、权重占净值比）重算，基金覆盖达 100% 骨架样本，时间跨度扩至 2006–2026。 |
| **de / pgr / plr** | 在全持仓上重算 | 46.3% 观测 / **99.8% 基金**（399/400） | `calc_de(only_612=True)` 在全持仓（444 只）上重跑；覆盖受限是**处置效应定义性**（仅半年报/年报全持仓可算 de），非数据缺口。 |
| **ARG** | 在全持仓上重算 | 96.7% 观测 / 100% 基金 | 已近乎全样本；2006–2017 老样本期受个股收益覆盖限制（见 P3）。 |
| **SDI** | 重算（滚动 8 期） | 63.8% 观测 / 91.5% 基金 | 暖机期导致早期无值，**结构性**，更长历史不改变。 |
| **AS_improved** | 重算 | 94.4% 观测 | 持仓驱动，已基本覆盖。 |
| **LSV / risk_asym / return_volatility** | 重算 | 92.1% / 72.0% / 92.0% 观测 | LSV 精度受混合频 top-10 限制（见一-B）；risk_asym 受 ≥4 季历史约束。 |
| **school** | （未变） | 45.9% 观测 / 63.2% 基金（253/400） | 基金经理毕业院校属**信息可得性限制**，需外部补全（见第三节）。 |

**复跑命令**：`指标计算流水线/run_all.py`（已执行，全 8 步通过）；ICI 切换后用 `02_L2_持仓偏离.py` + `99_合并面板.py` 重build 面板。

---

## 二、已落盘/新建文件清单

| 文件 | 状态 | 用途 |
| --- | --- | --- |
| `指标计算流水线/data/L3_交易行为层/基金换手率_双边_含卖出.csv` | **已恢复真实基座**（200 只/1799 真实值） | TO 主变量数据源（修复前为损坏脚手架，已另存 `.bak_broken`） |
| `指标计算流水线/data/L3_交易行为层/基金换手率_CSMAWind_双边.csv` | **已建空模板**（列 `fund_code,report_date,total_buy,total_sell,avg_aum`） | P1 CSMAR/Wind 双边换手率**落盘位**，下载后直接填充 |
| `指标计算流水线/data/L2_持仓偏离层/行业集中度HHI_申万31.csv` | **已切换为全样本**（444 只/2006–2026） | ICI/industry_hhi 数据源（旧文件已备份 `.bak_preFullSample`） |
| `指标计算流水线/数据扩展脚本/00_run_extension.py` | 已有（正式入口） | ICI/HHI 全样本重算的实际驱动（`extend_ici_hhi`）；独立脚本 `recompute_ici_fullsample.py` 本次会话落盘未持久化，但其结果已通过本入口写入 HHI 并刷新面板 |
| `指标计算流水线/数据扩展脚本/output/字段扩展_全样本.csv` | 由 `00_run_extension.py` 生成 | 6 组字段全样本对齐表（缺失=NaN） |
| `指标计算流水线/数据扩展脚本/output/字段扩展_覆盖率矩阵.csv` | 由 `00_run_extension.py` 生成 | 字段级覆盖率（**TO/DE/SDI/school/ARG 权威**；ICI 口径见下注） |

> **ICI 口径差异说明**：`字段扩展_覆盖率矩阵.csv` 中 ICI/HHI = **49.3% 观测**，是扩展脚本 `extend_ici_hhi` **仅按半年报/年报全持仓**计算的覆盖（97.6% 的半年观测 ÷ 全样本 9974 行）。而**最终主面板 `主分析面板_重建.csv`** 经 step 02 的 `calc_ici` 逻辑在 Q1/Q3 用 top-10 持仓补齐，ICI 在所有季度均有值，整体达 **94.3% 观测 / 100% 基金**——面板才是分析用数据集，故论文口径以面板 94.3% 为准。两者均真实，仅分母/口径不同。
| `指标计算流水线/数据扩展脚本/output/字段覆盖情况说明_补充.md` | 由 `00_run_extension.py` 生成 | 字段覆盖情况说明 |

---

## 三、仍需下载（CSMAR/Wind / iFinD，本环境无法执行）

以下为**确定性字段缺口**，必须在其授权终端导出后落盘到上述指定路径，再重跑 `run_all.py` / `00_run_extension.py` 即可刷新覆盖。精确提示词见 `数据下载提示词_2026-08-13.md`，此处仅列优先级与 impact：

1. **P1（最高优先）CSMAR/Wind 双边换手率** → 落盘 `基金换手率_CSMAWind_双边.csv`。
   - Impact：TO/OCI 从 **200 只 → 400 只 / 2006–2026 半年频**，直接消除 18% 主变量缺口。
   - 为什么不能免费做：AkShare 东财 `fund_portfolio_hold_em` 已不暴露逐股买卖金额（实测报错且曾核验接口非确定性）；其他免费源无基金买卖成交额。
2. **一-B（高优先）CSMAR/Wind 全季度全持仓** → 落盘 `基金持仓明细_CSMAWind_全季度全持仓.csv`。
   - Impact：① LSV 改用真实完整持仓方向（Q1/Q3 不再仅靠 top-10 推断），精度↑、解锁「市值分层羊群」条件检验；② 顺带提升 AS/ARG/ICI/DE 覆盖。
3. **P2（稳健性）iFinD 清盘/退市基金** → 落盘 `清盘基金净值_全量_iFinD.csv` + `清盘基金持仓_全量_iFinD.csv`。
   - Impact：生存偏差稳健性检验（与 400 只生存样本零交集，不计入主指标覆盖）。
4. **P3（中优先）个股月收益补全（2006–2017 老股/退市股）** → 增量合并进 `个股月收益率_全量.csv`。
   - Impact：改善 ARG/DE 在 2006–2017 老样本期的覆盖（当前 ARG 96.7% 已高，老期仍受 108 股子集残留影响）。
5. **school（控制变量，46%）外部院校补全** → 合并进现有 `school`。
   - Impact：基金经理毕业院校属**信息可得性限制**，难 100%；CSMAR 简历库/公开履历可补一部分。
6. **P4（可选）逐股买卖方向** → 落盘 `基金逐股交易方向.csv`。
   - Impact：严格复现 LSV 真实买卖方向与真实单边换手率（当前用持仓变动方向代理）。

---

## 三-B、本沙箱网络可行性实测（P3 亦无法在沙箱内免费抓取）

为确认 P3（个股月收益 2006–2015 补全）是否可在本环境直接下载，实测了全部可达免费源：

| 源 | 测试方式 | 结果 | 结论 |
| --- | --- | --- | --- |
| 东方财富 `push2his.eastmoney.com` | curl kline `klt=103` | `http=000`（DNS/连接失败） | **沙箱不可达** |
| 新浪 `getKLineData`（日线） | curl + 解析 | 仅返回**最近 ~6 年**日线，不支持起止日期 | 无法回溯到 2006 |
| 新浪 `hisdata/klc_kl.js` | curl + 解析 | 返回 **gzip/base64 编码串**（反爬），不可直接解析 | 已失效 |
| 腾讯 `web.ifzq.gtimg.cn` kline | curl | 返回 52 字节空错误体 | 不可用 |
| AkShare `stock_zh_a_hist` | Python（requests） | `RemoteDisconnected`（沙箱代理不继承给 requests） | 不可用 |
| AkShare `fund_portfolio_hold_em` | Python | 不再暴露逐股买卖金额，解码报错 | 不可用 |

**结论**：P3 与 TO（P1）、全季度全持仓（一-B）、清盘基金（P2）、逐股方向（P4）同属**需在其授权终端（CSMAR / Wind / iFinD，或用户本机 AkShare/新浪）导出**的字段，**无法在本沙箱免费补齐**。本环境已完成的全部为**本地可最大化**部分（基于既有的 `基金持仓明细_全量修正版_v2.csv` 与恢复后的 TO 真实基座重算）。

> 备注：现有 `个股月收益率_全量.csv` 仅覆盖 **2016-02 ~ 2026-07**（4200 只），2006–2015 整段缺失，正是 P3 要补的区间；其原始抓取依赖的新浪接口现已加密，须换源重抓。

---

## 四、关键方法论提示（写论文时必须说明）

- **ICI / DE 的"半年度限制"是监管披露约束**（仅半年报/年报披露全持仓），不是数据缺口；季度频 ICI 若用 top-10 估算会系统性低估集中度，不作主口径。
- **TO 主变量必须用 CSMAR/Wind 确定性字段**；东财拉长序列（含本次恢复的 200 只真实值以外的任何东财序列）已核验存在接口非确定性，不进主回归。
- **SDI 暖机、risk_asym 历史窗**为结构性，非缺口。
- 所有重算产物均**未破坏** `output/主分析面板_重建.csv` 既有口径；下载源就绪后重跑即刷新。

---

## 五、你下一步需在其授权终端执行的下载（本沙箱已留好落盘位与提示词）

按 `数据下载提示词_2026-08-13.md` 的精确提示词，在 CSMAR/Wind/iFinD（或用户本机 AkShare/新浪）执行，落盘到已建好的模板路径后重跑 `run_all.py` / `00_run_extension.py` 即可刷新覆盖：

1. **P1 双边换手率** → `基金换手率_CSMAWind_双边.csv`（模板已建，列名锁定）。TO/OCI 由 200 只 → 400 只。
2. **一-B 全季度全持仓** → `基金持仓明细_CSMAWind_全季度全持仓.csv`。提升 LSV 精度 + 解锁市值分层羊群检验。
3. **P3 个股月收益补全（2006–2015）** → 增量合并进 `个股月收益率_全量.csv`（现有文件仅 2016+）。需换源重抓（新浪原接口已加密）。改善 ARG/DE 老样本期覆盖。
4. **P2 清盘/退市基金** → `清盘基金净值_全量_iFinD.csv` + `清盘基金持仓_全量_iFinD.csv`（生存偏差稳健性）。
5. **school 基金经理院校** → 合并进现有 `school`（CSMAR 简历库/公开履历可补一部分，难 100%）。
6. **P4 逐股买卖方向** → `基金逐股交易方向.csv`（严格复现 LSV 真实方向）。

> 其余字段（ICI/DE/ARG/SDI/AS/LSV）本次已在本地最大化，无需额外下载即可达到当前最优覆盖。
