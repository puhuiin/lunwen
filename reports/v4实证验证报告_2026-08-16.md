# v4 实证结论独立验证报告（2026-08-16）

> 目的：对前序锁定的 **v4 双向聚类（CGM2011）M4 权威结果** 做独立审查与复验，确认"RiskAsym 与 DE 在渐近 / WCB / 置换三种口径下均显著，仅 LSV 不显著"的定稿经得起审视，并排查数据与算法层面任何潜在错误。
> 验证基于当前面板 `指标计算流水线/output/主分析面板_重建_含TOwind.csv`（MD5=`bc0942cec283fcb83307130530e7bd31`）。

## 1 · 可复现性（Reproducibility）

- 重跑 `_v4_benchmark_table.py`，将新生成 `_v4_benchmark.json` 与既有版本逐字段比对：
  - 标量（N=2264 / 基金=348 / R²=0.1290 / Adj.R²=0.1189 / ΔR²_L5=0.0352）：**完全一致**。
  - 18 列系数 β / SE / t / p（双向 + 单维）：**最大差 = 0.0**。
  - 置换 p、WCB p：**完全一致**。
- 结论：v4 基准**完全可复现**，不存在随机性或非确定性误差。

## 2 · 双向聚类 SE 与 linearmodels 金标准交叉验证（核心）

在手写 CGM2011 与 `linearmodels 7.0` 多向聚类（基金×年）上对**真实 M4 数据**逐一比对 18 个系数：

| 对照口径 | SE 差异（最大） | t 差异（最大） | 结论影响 |
|---|---|---|---|
| linearmodels homoskedastic（标准聚类，无小样本修正） | **0.00%** | **0.00%** | 手写算法与金标准**逐位相同** |
| linearmodels debiased（小样本自由度修正） | 3–6%（最大 6.14%） | 2–6% | L5 结论**全部不变** |

**L5 三指标在 debiased 修正下的 t 值（更保守）**：
- de：−2.994 → −2.826（仍 **p<0.01**）
- lsv：+0.770 → +0.727（仍 n.s.）
- risk_asym：+3.568 → +3.385（仍 ***）

**判定**：手写 CGM2011 双向聚类 SE 在数学上**完全正确**（与金标准 0.00% 差异）。记忆中"t 差~3%"实为 linearmodels `debiased` 小样本修正所致，幅度不足以翻转任何 L5 结论。v4 采用标准（无修正）聚类口径，是规范选择。

## 3 · WCB-S 与置换检验的 RNG 稳定性

`_recompute_robustness_v4` 原用固定种子 `default_rng(20260815)`（确定性）。为验证结论对随机实现稳健，改用 4 个不同种子复算（N_PERM=300 / B=499）：

| 指标 | 置换 p 区间 | WCB p 区间 | 三口径结论 |
|---|---|---|---|
| de | 0.040 – 0.047 | 0.006 – 0.014 | **均显著 (<0.05)** |
| lsv | 0.473 – 0.507 | 0.212 – 0.247 | **均不显著** |
| risk_asym | 0.020 – 0.047 | 0.000 | **均显著** |

- 原种子 20260815 的存储值被精确复现（de 置换 0.0467 / WCB 0.014；lsv 置换 0.5067 / WCB 0.229；RA 置换 0.02 / WCB 0.0）—确认管线确定性。
- **判定**：定稿结论对随机种子**稳健**，不受特定随机实现影响。

## 4 · L5 区块联合显著性（增量贡献）

- 受限模型（剔除 de/lsv/risk_asym）vs 完整 M4 的 ANOVA F 检验：**F=19.16, p=2.87e-12**。
- 判定：L5 三指标**联合高度显著**，ΔR²_L5=0.0352 是有实质意义的增量贡献（支持稿件"L5 行为层增量有效"的声称）。

## 5 · 数据健康度（诚实披露要点）

- 全部 18 个 RHS + DV 的缩尾列：**无 inf、无损坏值**。
- 样本保留：面板 9,974 行 → M4 回归 **2,264 行（降 77.3%）**，348 只基金。
- **瓶颈变量分析**：作为最后必需变量时的边际样本损失 —
  - `de`（处置效应）：损失 **2,161 行**（原始覆盖仅 46.6%）← 主导瓶颈
  - `SDI`：损失 620；`TO_wind`：损失 289；`risk_asym`：损失 275
- **诚实披露含义**：M4 估计样本是"DE 可计算"的**选择性子样本**（DE 依赖半年频全持仓快照的盈/亏分类，结构性缺失）。系数（尤其 DE 的显著负向）应在该选择条件下解释，不可外推至全部 9,974 观测。此局限须在主文稿 §4 / §6 方法局限处明确披露。
- 缩尾口径已核实：`_v4_benchmark_table.py` 第 25–29 行对**全部 RHS（含 FF5 五因子）及 DV** 独立按列做 1%/99% 缩尾 —— v4 修正核心（前序"未缩尾 FF5"错误）在代码中落实正确。

## 6 · 数据完整性备注（需关注但不影响结论）

- 面板当前 MD5=`bc0942ce…`，与项目记忆旧记 `f79b4900` **不符**。核查：面板于 **2026-08-16 07:45 被 run_all 重新生成过一次**；因 v4 基准对该面板完全可复现（§1），结论不受影响。已更新 MEMORY.md 记录真实 MD5 并注明重生成事件。
- 由此提示：**任何 M4 系数更新一律以 `_v4_benchmark.json` / `回归表_双向聚类_v4基准_2026-08-16.html` 为单一真相源**，并建议重跑前先 `certutil -hashfile` 记录面板 MD5，防止口径漂移。

## 7 · 总体判定

| 验证维度 | 结果 |
|---|---|
| 系数可复现 | ✅ 逐字段一致 |
| 双向聚类 SE 正确性 | ✅ 与金标准 0.00% 差异 |
| 结论对小样本修正稳健 | ✅ debiased 下 L5 结论不变 |
| WCB/置换 RNG 稳健 | ✅ 4 种子结论不变 |
| L5 联合显著性 | ✅ F=19.16, p=2.87e-12 |
| 数据无损坏/无 inf | ✅ |
| 样本选择性（DE 瓶颈） | ⚠️ 需强化诚实披露 |

**最终结论**：v4 定稿 — **RiskAsym（β=+0.07311, t=+3.57, p≈0.000）与 DE（β=−0.00606, t=−2.99, p=0.003）在渐近（双向聚类）/ WCB / 置换三种口径下均稳健显著；LSV（β=+0.01548, t=+0.77, p=0.441）在三种口径下均不显著** —— 经本轮独立审查，数据根基与算法均成立，定稿可信。唯一待办是**强化"M4 为 DE 选择性子样本"的诚实披露**。

---
*验证脚本：`_v4_benchmark_table.py`（重跑复现）、内联 linearmodels 交叉验证、内联多种子 WCB/置换复算、内联 L5 联合 F 检验。所有结果可由上述面板 + 脚本确定性复现。*
