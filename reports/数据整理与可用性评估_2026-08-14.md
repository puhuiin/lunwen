# 数据整理与可用性评估 — 2026-08-14 下载批次

> 目的：核对用户 2026-08-14 下载的 iFinD / CSMAR / Wind 文件落盘位置与可用性，映射到指标计算流水线的输入槽位，澄清路径歧义，并修正一处规模变量回归。
> 复核基准：`指标计算流水线/output/主分析面板_重建.csv`（9,974 观测 / 400 只 / 49 列，重跑于 2026-08-14）。

---

## 0. 路径澄清（先排雷）

用户消息写的落盘路径 `d:\Desktop\基金经理行为分研究\数据\L4_风险应对\` 是**笔误**。Desktop 下实际存在两个文件夹：

| 文件夹 | 状态 | 说明 |
|---|---|---|
| `基金经理行为分析研究` | **真实工作区** | 所有数据在此；L4 层实际名为 `数据/L4_风险应对层/`（带"层"后缀） |
| `基金经理行为研究` | 独立空文件夹 | 仅含 `代码/` 子目录，**无数据**，疑似旧副本/误建 |

**结论**：数据无需跨文件夹拷贝，无重复。整理工作全部在 `基金经理行为分析研究/` 内进行。

---

## 1. 已落地并生效的文件（流水线已消费）

| 文件 | 落盘位 | 作用 | 当前面板覆盖 |
|---|---|---|---|
| `行业集中度HHI_申万31.csv` | `指标计算流水线/data/L2_持仓偏离层/` | ICI / 申万31 行业集中度（**全样本 444 只 / 2006–2026**） | **ICI / industry_hhi = 94.3%（400 只）✅ 本次最大提升** |
| `基金换手率_双边_含卖出.csv` | `指标计算流水线/data/L3_交易行为层/` | 真·双边换手率 TO（**200 只真实买卖基座**，已恢复） | TO / OCI = **18%（200 只）✅ 已生效，但仅 200 只口径** |
| `主分析面板_重建.csv` | `指标计算流水线/output/` | 重跑后主面板 | 9,974 × 49，无 2026Q3+ 泄漏，无重键 |
| `清盘基金净值_全量_iFinD.csv` | `数据/L4_风险应对层/` | P2 清盘样本净值（生存偏差稳健性） | 25.7 MB / 364,555 行，待接入 |
| `清盘基金持仓_全量_iFinD.csv` | `数据/L4_风险应对层/` | P2 清盘样本持仓（由 `_明细_` 规范化生成） | 2,280 行 / 25 只 / 33 期，待接入 |

> `清盘基金持仓_全量_iFinD.csv` 由原始 `清盘基金持仓明细_iFinD.csv`（字段为 `hold_shares/hold_value`）按 `(fund_code, report_date)` 归一化为 `hold_ratio` 得到，以匹配 `lib_data_sources.py` 的预期 schema。原 `_明细_` 文件保留未动。

---

## 2. 空壳 / 待填槽位

| 文件 | 状态 | 说明 |
|---|---|---|
| `基金换手率_CSMAWind_双边.csv` | **空壳（55 B，仅表头）** | 全样本 TO 的 CSMAR/Wind 落盘槽。填入后 TO 可扩至 400 只。当前面板 TO 仍用 200 只基座。 |
| `基金经理院校_外部补全.csv` | **缺失** | school 当前 45.89%（253 只），补全后提升。 |

---

## 3. 代码写死的 4 个目标文件 — 落地进度（3 缺 1 已补）

| 目标文件 | 用途 | 状态 |
|---|---|---|
| `基金持仓明细_CSMAWind_全季度全持仓.csv` | **全样本处置效应 de**（真·全持仓权重） | ❌ **未下载** — de 当前 46.3%（399 只，仅半年频、302 只有持仓），该文件是解锁全样本 de 的唯一来源 |
| `清盘基金净值_全量_iFinD.csv` | P2 清盘净值稳健性 | ✅ 已下载（25.7 MB） |
| `清盘基金持仓_全量_iFinD.csv` | P2 清盘持仓稳健性 | ✅ 本次由 `_明细_` 规范化生成 |
| `基金经理院校_外部补全.csv` | school 补全 | ❌ **未下载** |

---

## 4. 关键修复：avg_aum 回归已修正（P1）

**问题**：用户"恢复 TO 真实基座"时，覆盖了此前 400 只规模增强，导致重跑后面板 `avg_aum` 跌回 **18%（200 只）**——规模控制变量失效，全样本回归被迫退回 200 只子集。

**修复**：
- 新增 `lib_metrics.calc_avg_aum()`：从 `基金规模历史_批量.csv` 的 `net_asset`（亿元，公开披露口径）按 `(fund_code, year, quarter)` 聚合，经骨架 `(year, quarter)→report_date` 唯一映射并入面板。
- 在 `99_合并面板.py` 中将 `avg_aum` 从 L3（200 只 TO 子集）**解耦**，改为独立并入全样本规模。
- 已把规模文件拷入流水线 `data/L1_背景特征层/`（与其他 L1 文件同目录，保持流水线自包含）。

**结果**：`avg_aum` 恢复至 **99.93%（400 只）**，面板重跑完成（9,974 × 49，无泄漏、无重键）。规模控制变量现在可用于全样本回归，"含/不含 TO" 双口径设计恢复可用。

---

## 5. Wind TO 抓取子代理结果

- 此前启动的 20 个并行 Wind 子代理（`代码/wind_to/chunks/chunk_00–19`）**未产出任何 `_out.csv`**——抓取在用户发消息时中断，且 Wind `get_fund_holdings` 仅返回前 10 持仓、换手率需逐基金半年度，免费源不可行。
- **已被用户手动下载的 TO 基座取代，无需重跑。**

---

## 6. 数据缺口与论文可启动性（更新版）

**重跑后覆盖矩阵**（9,974 观测 / 400 只）：

| 层 | 指标 | 覆盖 | 备注 |
|---|---|---|---|
| 背景 | avg_aum | **99.9%** ✅ | 本次修复 |
| 背景 | mgr_total_tenure | 99.1% | |
| 背景 | log_fund_age | 100% | |
| 背景 | school | 45.9% ⚠️ | 待外部补全 |
| 持仓偏离 | ICI / industry_hhi | **94.3%** ✅ | 全样本（444 只）本次生效 |
| 持仓偏离 | AS_improved | 94.4% ✅ | |
| 交易行为 | TO / OCI | 18% ⚠️ | 200 只真实基座，口径受限 |
| 交易行为 | SDI | 63.8% | 结构性滚动 8 窗暖机 |
| 风险 | ARG | 96.7% ✅ | |
| 风险 | return_volatility | 92.0% ✅ | |
| 认知 | de | 46.3% ⚠️ | 半年频、仅 302 只有持仓 |
| 认知 | lsv | 92.1% ✅ | |
| 认知 | risk_asym | 72.0% | 需 ≥4 季历史 |
| 因变量 | quarter_return | 100% | |
| 因变量 | future_return | 96.0% | 2026Q2 自然 NaN |
| 因变量 | RG | 87.3% | |

**泄漏审计**：`report_date` 上限 2026-07-01（2026Q2）；无 `q_end > 2026-09-30` 行；`future_return` 在 2026Q2 NaN。✅ 无未来季度泄漏。

**结论：维持 Conditional Go。**
- 两处最大提升（avg_aum 全样本、ICI 全样本）已完成，规模/持仓偏离控制变量现已接近全样本，可支撑全样本横截面与组内回归。
- 不阻塞论文的**文献综述 / 理论框架 / 假设推导 / 变量定义 / 描述性统计（2018–2026 子样本）**写作。
- 硬缺口仍卡在三类：**TO（200 只口径，需 CSMAR/Wind）**、**de（需全季度全持仓）**、**school（需外部补全）**——这些不影响方法章节，但限制主分析样本宽度与部分稳健性。

---

## 7. 待办清单（下载 / 处理）

| # | 任务 | 优先级 | 说明 |
|---|---|---|---|
| 1 | 下载 `基金持仓明细_CSMAWind_全季度全持仓.csv` | **P0** | 解锁全样本 de，是剩余最大数据缺口 |
| 2 | 补全 `基金经理院校_外部补全.csv` | P2 | school 提至全样本 |
| 3 | （可选）填 `基金换手率_CSMAWind_双边.csv` | P2 | TO 扩至 400 只；或维持 200 只子集并显式说明口径 |
| 4 | 清盘样本接入 | P2 | 标准化 `fund_code`（NAV 用 `.OF`、持仓用 6 位）后跑生存偏差稳健性 |
| 5 | 清理 `基金经理行为研究` 独立空文件夹 | P3 | 确认是否废弃，避免与真实工作区混淆 |

---

*生成：2026-08-14 · 复核脚本 `代码/audit_panel_current.py` · 面板 `指标计算流水线/output/主分析面板_重建.csv`*
