# -*- coding: utf-8 -*-
"""第二轮定点修正: 补齐遗漏的 DE/RA 描述统计覆盖率/基金数, 以及行125/1313 的 v20 版本标签。"""
import io, os

SRC = r"D:\Desktop\基金经理行为分析研究\merged_manuscript.html"
with io.open(SRC, "r", encoding="utf-8") as f:
    t = f.read()
orig = t

repls = [
    # 行384: DE 覆盖率叙述
    ("覆盖355只基金。由于持仓数据的披露频率和完整性限制，DE指标的覆盖率为47.3%。",
     "覆盖385只基金。由于持仓数据的披露频率和完整性限制，DE指标的覆盖率为46.5%。"),
    # 行425: §3.6 L5 覆盖概览
    ("DE 42.7%（4,262）", "DE 46.5%（4,642）"),
    # 行444: RA 覆盖率叙述
    ("覆盖387只基金，RiskAsym的覆盖率为66.0%。", "覆盖386只基金，RiskAsym的覆盖率为72.0%。"),
    # 行531: §3.8 描述表 DE 行
    ("47.3%（355基金）", "46.5%（385基金）"),
    # 行533: §3.8 描述表 RA 行
    ("66.0%（387基金）", "72.0%（386基金）"),
    # 行747: §4.2.7 覆盖率说明
    ("DE 42.7%, LSV 92.1%, RiskAsym 72.0%", "DE 46.5%, LSV 92.1%, RiskAsym 72.0%"),
    # 行1314: Ch6 DE 功效叙述
    ("DE功效不足的主要原因是其覆盖率（47.3%）", "DE功效不足的主要原因是其覆盖率（46.5%）"),
    # 行1397: Ch6 §6.4.2 覆盖率
    ("RiskAsym 72.0%、LSV 92.1%、DE 42.7%", "RiskAsym 72.0%、LSV 92.1%、DE 46.5%"),
    # 行125: Ch1 路线图 v20 标签
    ("第四章 实证分析</strong>：基于v20面板数据，采用截面基准",
     "第四章 实证分析</strong>：基于当前可复现面板数据（run_full.py 统一口径重建），采用截面基准"),
    # 行1313: Ch6 v20 标签
    ("数据扩展至v20面板后，三个L5指标在M4完整模型中同时达到统计显著",
     "在当前可复现面板的M4完整模型中，三个L5指标同时达到统计显著"),
]

for o, n in repls:
    assert o in t, "NOT FOUND: " + o[:50]
    t = t.replace(o, n)

with io.open(SRC, "w", encoding="utf-8") as f:
    f.write(t)

# 校验
print("LEN", len(orig), len(t))
for bad in ["47.3%", "42.7%", "66.0%", "355基金", "355只", "387基金", "387只", "4,262", "基于v20面板", "扩展至v20面板"]:
    print(f"  residual {bad!r}: {t.count(bad)}")
print("done")
