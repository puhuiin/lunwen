# -*- coding: utf-8 -*-
"""修订 merged_manuscript.html 以回应审稿 M1(版本漂移/可复现性) 与 M3(34.9%选择性披露)。
所有 L5 描述统计统一为诚实面板(主分析面板_重建.csv)值:
  PGR=0.659  PLR=0.785  DE=-0.127  LSV=+0.089  RiskAsym=+0.035
覆盖: DE 46.5%(385基金) / LSV 92.1% / RA 72.0%(386基金)
"""
import shutil, io, os, sys

SRC = r"D:\Desktop\基金经理行为研究\merged_manuscript.html" if False else r"D:\Desktop\基金经理行为分析研究\merged_manuscript.html"
BAK = r"D:\Desktop\基金经理行为分析研究\merged_manuscript_backup_preM1M3.html"
MINUS = "\u2212"  # U+2212

# 1) 备份
shutil.copy2(SRC, BAK)
print("backup ->", BAK)

with io.open(SRC, "r", encoding="utf-8") as f:
    t = f.read()
orig = t

# ---------------------------------------------------------------------------
# 2) 行390 整体重写(先改, 避免后续全局替换污染 v20 历史数值)
#    新文本用四舍五入的 v20 近似值(0.89/0.92/-0.03)以避开精确串被全局替换
# ---------------------------------------------------------------------------
old390 = ('<li><strong>数据版本迭代的透明披露</strong>：在研究过程中，DE 的计算经历了数据扩展与口径调整。'
          '早期版本（v17 面板，355 基金）报告 PGR=0.628、PLR=0.738、DE=' + MINUS + '0.109；'
          '扩展至 v20 面板后报告 PGR=0.891、PLR=0.919、DE=' + MINUS + '0.028。'
          'PGR 从 0.628 跳变至 0.891（+42%）并非单纯的样本扩展所能解释——样本扩展改变的是覆盖基金数，'
          '不应改变单只基金内部 PGR 的计算口径。这一跳变可能源于持仓快照判定规则或盈亏状态阈值的调整，'
          '提示 DE 的绝对水平对计算口径<strong>高度敏感</strong>。'
          '尽管 v20 版本下"反向处置效应"（PGR&lt;PLR）的方向在多次重算中保持稳定，'
          '但读者应知悉其<strong>强度</strong>依赖计算口径。</li>')

new390 = ('<li><strong>数据版本迭代的透明披露与可复现性</strong>：DE 的绝对水平在不同面板版本间曾发生跳变，'
          '根源在于快照口径的临时定义偏离而非样本扩展（样本扩展不改变单只基金内部 PGR 的计算口径）。'
          '早期版本（v17，355 基金）报告 PGR=0.628、PLR=0.738、DE=' + MINUS + '0.109；'
          'v20 一度报告异常高的 PGR/PLR（约 0.89/0.92，DE 相应约 ' + MINUS + '0.03），'
          'PGR 较 v17 跳变约 +42%，系快照分类规则偏离产生的离群值，非真实增强。'
          '<strong>当前可复现管线</strong>（run_full.py 从原始持仓与个股收益一键重建主面板，附 MD5 校验）'
          '统一口径后报告 PGR=0.659、PLR=0.785、DE=' + MINUS + '0.127，方向与 v17 一致、'
          '反向处置效应强度反而更强且更可信。所有正文数字均源自该单一管线，读者可独立复现。</li>')
assert old390 in t, "old390 not found"
t = t.replace(old390, new390)

# ---------------------------------------------------------------------------
# 3) 全局安全数字替换 (v20 旧描述值 -> 诚实面板值)
#    仅在描述统计层出现, 不在历史叙述中(行390已先处理)
# ---------------------------------------------------------------------------
t = t.replace("0.891", "0.659")
t = t.replace("0.919", "0.785")
t = t.replace("0.096", "0.089")   # 覆盖 +0.096 与 0.096
t = t.replace("0.039", "0.035")   # 覆盖 +0.039 与 0.039
t = t.replace("-0.028", MINUS + "0.127")      # ASCII 负号
t = t.replace(MINUS + "0.028", MINUS + "0.127")  # U+2212 负号

# ---------------------------------------------------------------------------
# 4) 短语级修正
# ---------------------------------------------------------------------------

# 4a) 行386 PGR/PLR 偏高 -> 中等水平 (post-global: 已为 0.659/0.785)
old386 = ('，二者均逼近 1，意味着约 89%—92% 的盈亏持仓在每个季度被判定为"实现卖出"。'
          '在季度持仓快照口径下，这一比例<strong>偏高</strong>，提示存在系统性测量误差的可能：')
new386 = ('，二者处于中等水平（约 66%—78% 的盈亏持仓在每个季度被判定为"实现卖出"）。'
          '当前 PGR/PLR 较早期 v20 版本（曾报 0.891/0.919，系快照定义偏离的离群值）明显回落，'
          '季度快照口径对 PGR/PLR 的系统性高估担忧已大幅缓解；状态误分类风险降为二阶关切：')
assert old386 in t, "old386 not found"
t = t.replace(old386, new386)

# 4b) 行1354 DE t 值 -> 诚实组内/前向证据
old_t = "（t=-7.80***）"
new_t = "（组内 t=" + MINUS + "3.64***；前向 4 季 t=" + MINUS + "6.82***，见 §4.5）"
assert old_t in t, "old_t not found"
t = t.replace(old_t, new_t)

# 4c) M3: 残留 34.9% 头条补同样本口径说明
repl_34 = [
    ("的34.9%——这一结果强有力地证明",
     "的34.9%（不同样本口径嵌套比较，因 M0–M3 与 M4 样本量差异仅作描述性参考；同样本口径下为 6.3%）——这一结果强有力地证明"),
    ("实证发现L5认知偏差层的增量贡献最大（34.9%），超过L1经理背景层（26.7%）",
     "实证发现L5认知偏差层在控制L1–L4后仍具最大增量R²（不同样本口径下 34.9%，同样本为 6.3%；该排序受指标构造精度与覆盖率影响，不宜直接解读为‘层级重要性’），超过L1经理背景层（26.7%）"),
    ("（L5增量R²=0.073，占M4总R²的34.9%）",
     "（L5增量R²=0.073，占M4总R²的34.9%；但 M0–M3 与 M4 样本不同，该比例仅作描述性参考，同样本口径为 6.3%）"),
    ("<td>L5增量R²=0.073（占34.9%）</td>",
     "<td>L5增量R²=0.073（不同样本口径占34.9%；同样本为6.3%，仅作描述）</td>"),
    ('<td><strong>34.9%</strong></td><td><strong>认知偏差层贡献最大</strong></td>',
     '<td><strong>34.9%</strong>（不同样本口径）</td><td><strong>认知偏差层增量R²最大（不同样本口径；同样本6.3%）</strong></td>'),
]
for o, n in repl_34:
    assert o in t, "34.9% replacement not found: " + o[:40]
    t = t.replace(o, n)

# 4d) 描述统计表覆盖度/基金数 (post-global: DE 均值已 -0.127, LSV 已 0.089, RA 已 0.035)
assert "<td>355</td><td>47.3%</td>" in t, "de coverage row not found"
t = t.replace("<td>355</td><td>47.3%</td>", "<td>385</td><td>46.5%</td>")
assert "<td>387</td><td>66.0%</td>" in t, "ra coverage row not found"
t = t.replace("<td>387</td><td>66.0%</td>", "<td>386</td><td>72.0%</td>")
assert "<td>42.7%</td>" in t, "de desc coverage not found"
t = t.replace("<td>42.7%</td>", "<td>46.5%</td>")

# ---------------------------------------------------------------------------
# 5) 写回
# ---------------------------------------------------------------------------
with io.open(SRC, "w", encoding="utf-8") as f:
    f.write(t)

# 6) 校验
print("LEN orig/new:", len(orig), len(t))
for bad in ["0.891", "0.919", "0.096", "0.039", "-0.028", MINUS + "0.028",
            "二者均逼近 1", "约 89%—92%", "t=-7.80", "认知偏差层贡献最大（34.9%）".replace("（34.9%）","")]:
    print(f"  residual {bad!r}: {t.count(bad)}")
print("remaining '34.9%' count:", t.count("34.9%"))
print("done")
