# 运行顺序与可复现性说明（数据处理 → 回归）

> 目标：在任意机器（含你本机）上「手动依次运行代码」，得到与我（本会话）**完全一致**的论文面板与回归结果。  
> 最后核验：2026-08-15 在本机沙箱端到端跑通 `run_all.py`（10 步全 OK）+ 诚实回归。SE 已升级为**双向聚类(fund×year)**，SDI H1 **t2w=+2.46\*\***（单维对照 t1w=+12.41\*\*\*）；面板二次重跑 MD5 一致（确定性已验证）。

---

## 一、两条运行路径（任选其一）

### 路径 A：一键复现（推荐）

在项目根目录（`基金经理行为分析研究/`）执行：

```bash
python run_full.py
```

它会自动按顺序执行：

1. `指标计算流水线/run_all.py`  → 产出论文主面板 `主分析面板_重建_含TOwind.csv`
2. `L3_L1_regressions_HONEST_2026-08-14.py` → 产出回归结果 `L3_L1_regression_HONEST_TOWind_2026-08-15.json`

### 路径 B：分步手动运行（便于排查/单步核对）

```bash
# 1) 数据处理流水线（10 步，顺序固定，任一步失败即中止）
python 指标计算流水线/run_all.py

# 2) 回归分析（读含 TO_wind 的论文面板）
python L3_L1_regressions_HONEST_2026-08-14.py
```

> 你本机用哪个 python 都行，只要装了 `pandas / numpy`（回归脚本用 `scipy` 计算 t 分布，建议一并安装；建议 pandas≥2.0）。  
> 本会话验证用的环境：托管 venv `C:\Users\26955\.workbuddy\binaries\python\envs\default\Scripts\python.exe`（pandas 3.0.5 / numpy 2.4.6 / scipy 1.15）。双向聚类实现已与 `linearmodels` 交叉验证。

---

## 二、关键产物（运行后落盘位置）

| 文件                | 路径                                                 | 说明                                        |
| ----------------- | -------------------------------------------------- | ----------------------------------------- |
| 论文主面板（含 Wind 换手率） | `指标计算流水线/output/主分析面板_重建_含TOwind.csv`              | 9974 行 × 51 列，400 只基金，2006Q2–2026Q2 非平衡面板 |
| 论文主面板（不含 TO_wind） | `指标计算流水线/output/主分析面板_重建.csv`                      | 9974 × 49，99_合并面板产出，07b 在其上并入 TO_wind     |
| 回归结果 JSON         | `L3_L1_regression_HONEST_TOWind_2026-08-15.json`   | H1–H5 各规格的 β / t / p / 显著性星               |
| 长表换手率             | `指标计算流水线/data/L3_交易行为层/基金换手率_Wind_EDE20260815.csv` | Wind EDE 换手率长表（仅 400 只面板基金）               |
| 中间层 CSV           | `指标计算流水线/output/L1_背景特征.csv … L6_因变量.csv`          | 各层指标，99 合并用                               |

---

## 三、回归锚点（你跑出来应与下表逐位一致）

方法学：双向 demeaning（fund_code + year）+ **双向聚类稳健标准误（fund×year，Cameron–Gelbach–Miller 2011）**；DV = `quarter_return`（时变，非 FF5 截距）。t2w = 双向聚类 t（**主口径**）；t1w = 单维基金聚类 t（对照列，供透明度参考）。

| 规格  | 主变量                     |                               β | t2w(双向) | 显著性 | t1w(单维·对照) |              样本 |
| --- | ----------------------- | ------------------------------: | ------: | :---: | :----------: | --------------: |
| H1  | SDI（真算风格漂移）             |                        +0.10280 |  +2.46  |  \*\* |   +12.41\*\*\* | N=6337 / 366 基金 |
| H2  | TO_wind（Wind 集成换手率）     |                        +0.00003 |  +1.78  |   \*  |    +3.17\*    | N=8480 / 394 基金 |
| H2b | TO_two_sided（旧双边，稳健性对照） |                        −0.00446 |  −0.17  |  ns   |    −0.27     | N=1788 / 200 基金 |
| H3  | OCI_two_sided（真算）       |                        −0.00143 |  −0.45  |  ns   |    −0.80     | N=1788 / 200 基金 |
| H4  | SDI + TO_wind + OCI（联合） | SDI=+0.01980 / TO_wind=+0.00002 | +3.09 / +0.56 | \*\*\* / ns | +2.58\*\* / +0.50 | N=1192 / 200 基金 |
| H5  | SDI + 控制（不含 TO，大样本）     |                        +0.10280 |  +2.46  |  \*\* |   +12.41\*\*\* | N=6337 / 366 基金 |

> **与旧快照/此前"压力测试"的差异澄清（重要）：** 此前 H1 报道的 `t=+12.38`（或"双向 12.68"）实为**单维基金聚类**口径（即上表 t1w 列），年份共同冲击未被吸收，估计过度乐观。正确**双向聚类(fund×year)** 下 `t2w=+2.46`，SDI 仍显著为正（5%），但由 1% 收敛至 5%——这是 Petersen(2009) 对金融面板的规范做法，年份维捕获了基金聚类未吸收的年度共同冲击。H2 TO_wind 在双向口径下仅边际显著（p=0.075，10%）。

### 边界与披露（Boundary A 及方法学）

- **SDI 有效识别窗口 = 2022+。** SDI 采用 window=8 滚动 OLS，首个有效 SDI 约 2022Q2；实证有效样本期 2022Q2–2026Q2。更早季度因暖机不足为空，不可外推至 2022 年前。
- **SDI(t) 与 quarter_return(t) 同期同位置对齐、无前视偏差。** 已独立复算核验（与 `lib_metrics.calc_sdi` 输出 diff=0）：SDI(t) 完全由截至 `t−1` 季度的收益估计，未使用 t 季或未来任何收益；在面板中按 `report_date` 与 `quarter_return(t)` 对齐。故 H1/H2 应表述为「同期关联」而非「预测」。
- **H1/H5（366 只）与 H4（200 只）是不同基金总体。** H4 因纳入 OCI_two_sided（仅 18% 覆盖）被限制在 200 只双边换手子集，其 SDI 系数（β=+0.020）与 H1（β=+0.103）不可直接比较——样本选择不同，非同一总体。
- **换手率覆盖差异：** TO_wind 86.6%（394 只，半年度，单边口径）；TO_two_sided / OCI_two_sided 仅 18%（200 只，半年度）。低覆盖变量构成 H2b/H3/H4 的样本约束。
- **双向聚类已交叉验证：** 自实现 CGM(fund×year) 与 `linearmodels` 双向 FE+双向聚类的 β 完全一致（0.09911）、t 仅差 ~3%（小样本校正差异），实现正确。
- **H4 受限样本个别控制 SE 不稳定：** 如 gender_m 在 H4 中 t=14.4（H1/H5 仅 0.75），系 200 基金/1,192 观测下少簇所致，不影响 SDI 头条结论（t=3.09\*\*\*）。H4 以 SDI 为准。

> 主换手率口径已统一为 **TO_wind**（图书馆 Wind 导出，**单边口径 = MAX(买入,卖出)/AVG市值**；合并 `EDE2026081d5.xlsx` 近期窗 2015H1–2026H1 + `EDE20260815(2).xlsx` 历史窗 2005H1–2015H1，覆盖 86.6% / 394 只，连续 2005H1–2026H1）；旧 `TO_two_sided` 仅 18% 且实为归一化交易额，仅作稳健性对照。

---

## 四、可复现性已落实的改动（本会话）

1. **路径全部可移植**：`run_all.py` / `07b_并入Wind换手率.py` / `L3_L1_regressions_HONEST_*.py` / `run_full.py` 一律用 `os.path.dirname(os.path.abspath(__file__))` 推导路径，**文件夹移到任何盘符/路径都能跑**，不再有硬编码 `D:/...`。
2. **`run_all.py` 补全缺失步骤**：原漏跑 `07_整合持仓数据.py`（v2 全持仓，02/03/04 依赖）与 `07b_并入Wind换手率.py`（TO_wind）。现已含完整 10 步顺序：07→00→01→02→03→04→05→06→99→07b。
3. **修复 `07_整合持仓数据.py` 的 `load_ifind` bug**：原代码按 `report_date` 列读取 iFinD 持仓，而实际文件是 `季度` 列（与 akshare 同源），导致 `KeyError` 直接崩溃（你手动跑必报错）。已改为与 `load_akshare` 相同的 `季度` 解析 + 市值代理占净值比例。**修复后重算的 v2 与修复前磁盘上的 v2 逐键完全一致（428,571 行、446 只、交集 100%）**，非破坏性。
4. **单一入口 `run_full.py`**：一键跑完「数据处理 → 回归」，避免漏步。
5. **备份**：修复/重跑前已备份原面板与 v2 至 `._repro_backup/20260815_133543/`、`20260815_v2_beforefix.csv`，供比对。

---

## 五、关于「与 8-14 快照不一致」的说明（诚实披露）

本次重跑的 `主分析面板_重建.csv` 与 2026-08-14 20:05 的旧快照 MD5 不同，原因是**源数据/代码在 8-14 之后有演进**，非计算错误、非不确定：

- `个股月收益率_全量.csv`（个股月收益，ARG/de/pgr/plr/lsv/RG 的来源）于 **2026-08-15 更新** → 这些指标相对旧快照有变动。
- 经理特征（school/education/gender/CFA）经严格键对齐复核，相对旧快照 **diff=0（逐格一致）**——前期"calc_control_vars 精炼致变动"系含TOwind面板与基础面板混比造成的伪差异，已作废。

**这两类变动都是「当前代码 + 当前数据」的正确结果**，且已验证：

- v2 持仓逐键一致；
- 管线各步确定性（无随机种子、groupby/merge 稳定）；
- 二次重跑面板 MD5 一致 → 你手动跑会得到与本会话**完全相同**的当前面板与回归锚点。

---

## 六、手动运行核对清单（跑完自查）

- [ ] `指标计算流水线/output/主分析面板_重建_含TOwind.csv` 存在，形状 (9974, 51)
- [ ] 终端打印 `TO_wind(原始%) 非空: 8642 (86.6%)`
- [ ] `L3_L1_regression_HONEST_TOWind_2026-08-15.json` 生成
- [ ] H1 SDI β≈+0.1028、t2w≈+2.46、\*\*（双向聚类主口径）；t1w≈+12.41\*\*\*（单维对照，与本文第三节逐位一致）
- [ ] 若改用不同 pandas/numpy 版本，β/t 末位小数可能浮动 1e-3 量级，但符号与显著性不变

---

## 七、常见报错与处理

| 现象                                              | 原因                      | 处理                                                   |
| ----------------------------------------------- | ----------------------- | ---------------------------------------------------- |
| `KeyError: 'report_date'`（07 步）                 | 旧 `load_ifind` bug（已修复） | 用本会话修复后的 `07_整合持仓数据.py`                              |
| `ModuleNotFoundError: No module named 'pandas'` | 当前 python 没装依赖          | 用装有 pandas/numpy/statsmodels 的解释器                    |
| `FileNotFoundError` 指向中文目录                      | 路径硬编码（已根除）或工作目录不对       | 确认用 `run_full.py` / `run_all.py`，不要手动 `cd` 进子目录改相对路径 |
| 回归报「样本不足跳过」某规格                                  | 该规格有效样本 < 阈值            | 正常（如 H4 联合样本较小），不影响主结论                               |
